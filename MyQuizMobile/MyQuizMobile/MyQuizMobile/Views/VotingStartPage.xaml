<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MyQuizMobile"
             x:Class="MyQuizMobile.VotingStartPage"
             Title="Abstimmung starten"
             Padding="5">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="listView" ItemsSource="{Binding ItemCollection}" VerticalOptions="Fill">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding DisplayText}" Detail="{Binding DetailText}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <StackLayout HorizontalOptions="Fill" VerticalOptions="EndAndExpand">
                <Grid HorizontalOptions="FillAndExpand">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="7*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Switch x:Name="personenbezogenSwitch" HorizontalOptions="FillAndExpand"
                            IsToggled="{Binding IsPersonal}" Grid.Column="0" Grid.Row="0"
                            IsEnabled="{Binding GroupHasSingleTopics}" />
                    <Label Text="Personenbezogene Fragen" HorizontalTextAlignment="Start"
                           VerticalTextAlignment="Center" HorizontalOptions="Start" Grid.Column="1" Grid.Row="0" />
                    <Entry x:Name="timeEntry" Text="{Binding TimeInSeconds}" Keyboard="Numeric"
                           HorizontalTextAlignment="End" HorizontalOptions="FillAndExpand" Grid.Column="0" Grid.Row="1" />
                    <Label Text="Sekunden pro Frage" HorizontalTextAlignment="Start" VerticalTextAlignment="Center"
                           HorizontalOptions="Start" Grid.Column="1" Grid.Row="1" />
                </Grid>
                <Button x:Name="weiterButton" Text="Weiter" HorizontalOptions="FillAndExpand"
                        IsEnabled="{Binding CanSend}" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>